# books-api
Stack implementation using Micro service architecture

- To run locally 
`npm run start`

- Run with Docker Compose :

`dockerc-compose up`

- K8 Implementation: 
    - Web API
    - MariaDB 
    - Persistent Volumes

    `kubectl apply -f k8s/`